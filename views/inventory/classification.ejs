<!-- Content Heading -->
<% if (title) { %>
  <h1><%= title %></h1>
<% } else {
  res.redirect('/')
} %>

<!-- Message Block (for future use) -->
<%# messages() %>

<!-- Inventory Items Display -->
<div class="inventory-container">
  <%- grid %>
</div>